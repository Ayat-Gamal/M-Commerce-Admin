query TestProducts($first: Int!) {
    products(first: $first) {
        edges {
            node {
                id
                title
                status
            }
        }
        pageInfo {
            hasNextPage
            endCursor
        }
    }
} 